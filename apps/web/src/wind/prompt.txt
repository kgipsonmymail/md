

我们新开发的内容放到wind目录下，并且我们的目标是开发中间件，这个排版和语法是别人设计的，我们的中间件要基于他们的语法和工具用法，即使未来换成另一个排版器，也能继续实现我们的需求。
我们主要需要的几个功能就是：
1. 转译代码的语法，让AI理解后做成排版
2. 渲染预览，把代码变成效果图
3. 导出兼容，渲染结果直接被公众号使用
4. 自动上传，另外一个项目可以把渲染的结果导入到公众号


最终目的是：
input原文和图片素材，
中间有：AI根据语法对文章重新排版，根据文章内容，活用各种格式来提高可读性，只要探索出一个统一的风格就可以大量使用；
output可以上传到公众号的预览和可复制的文本

现在这个项目我成功使用了：
1. 输出的内容可以无损复制到公众号：渲染即所得
2. md语法可以成功渲染，并且满足公众号的现有功能
3. 我手动让一个AI模型来调整排版，这个未来可以专门调用 API来实现，需要打磨好一套提示词。
这里接入的api可以来自dify的模型。

下一步需要的是：
1. 如果我上传的doc文件，能否自动提取其中的图片素材，并上传到图床，然后在文本中在图片位置替换为图片链接？（这里使用github图床服务，还需要对图床的目录结构进行管理。）GitHub生成的原始URL格式为：
https://raw.githubusercontent.com/[用户名]/[仓库名]/[分支名]/[路径]
2. 如果1不行，那么我只能图片和文本分开上传，但是这样图片顺序就很乱了。
3. 

应用可集成：
本项目给了一个非常干净完整的md代码与渲染结果页面，
我们可以开发一个纯文本编辑界面，并且允许把图片上传，用户把原文本和图片按顺序放入该编辑器。
调用api，让一个AI来对文本进行加工，图片处理成图床链接，按原文顺序插入并排版。
把排版结果自动放到原项目的非常干净完整的md代码与渲染结果页面，的md框，并渲染出来。这样用户等待一会就能拿到结果了！


cdn和图床项目也单独用一个文件夹的子路径专门管理。不要和其他地方弄混了。
资源：
方案1：jsDelivr免费CDN（推荐）
jsDelivr是GitHub官方推荐的CDN服务商，提供全球加速服务。使用方法：

将原始URL中的raw.githubusercontent.com替换为cdn.jsdelivr.net
添加@latest参数自动指向最新提交
示例转换：

原始URL: https://raw.githubusercontent.com/user/repo/main/images/2023/demo.png
CDN URL: https://cdn.jsdelivr.net/gh/user/repo@latest/images/2023/demo.png
优势：

全球200+节点，平均加载速度提升3-5倍
支持版本锁定（如@v1.0.0）
每月100TB免费流量


todo：
1. 学习使用1~2个图床平台